# Netlify (FREE) — CMS-only deploy (no Ruby build)
# This avoids Netlify compiling Ruby/Jekyll. GitHub Pages remains the public site builder.
# Netlify is used for: Identity + Git Gateway + Decap CMS at /admin.

[build]
  publish = "."
  command = "echo 'Netlify CMS-only deploy (no build)'"
